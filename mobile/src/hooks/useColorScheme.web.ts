/**
 * BNA UI compatibility: web-specific useColorScheme (hydration-safe).
 * Re-exports from use-color-scheme.web for components that import from '@/hooks/useColorScheme'.
 * @see https://ui.ahmedbna.com/docs/installation
 */
export { useColorScheme } from './use-color-scheme.web';
